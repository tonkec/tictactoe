import { Games } from '../../components/Games';
import { Layout } from '../../ui/layout';

const Home = () => {
    return (
        <Layout>
            <Games />
        </Layout>
    );
    }

    export { Home };